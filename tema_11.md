# Тема 10. Итераторы и генераторы
Отчет по Теме #11 выполнил:
- Боровягин Данил Андреевич
- ПИЭ-21-2

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | + |

Работу проверили:
- к.э.н., доцент Панов М.А.

## Лабораторная работа №1

### Простой итератор, но у него нет гибкой настройки, например его нельзя развернуть. Он работает просто как next(), но нет prev()

```python
num = [1,6,2,4,5]
for i in num:
    print(i)

```


### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_11/lab_11/1.jpg)

## Выводы

В данном коде мы видим как работает простой итератор, главным его минусом является отсутствие гибкой настройки. 

## Лабораторная работа №2

### Класс итератор с гибкой настройкой и удобными применением

```python
class Count:
    def __init__(self, start):
        self.Count = start + 1

    def __iter__(self):
        return self

    def __next__(self):
        self.Count -= 1
        if self.Count < 0:
            raise StopIteration
        return self.Count


user = int(input("введите число "))

counter = Count(user)

for i in counter:
    print(i)

```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_10/lab_11/2.jpg)

## Вывод

В данном коде мы видим какие бывают гибкие настройки для итератора, а именно мы можем настроить вывод чисел с определённым шагом. Создав класс перед итератором. Так же можем добавить ввод числа от пользователя. 


## Лабораторная работа №3

### Генератор списка

```python
a = [i ** 4 for i in range(0,4)]

print(a)

for i in a:
    print(i)

iterator = iter(a)

print("первый элемент списка - ", next(iterator))
print("слудующий элемент списка - ", next(iterator))
print("слудующий элемент списка - ", next(iterator))
print("слудующий элемент списка - ", next(iterator))

```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_10/lab_11/3.jpg)

## Вывод

В данном коде мы рассмотрели генератор списка, так же увидели новые возможности итерирования, а именно мы можем не писать цикл `for`, а просто использовать троенную фичу `iter()` вообще не обходимость в ней возникает чаще тогда когда нам необходимо получать элементы посредством `next`. в остольное время мы можем использовать как `for` так и `iter()`. разницы особой нет.


## Лабораторная работа №4

### Выражения генераторы 

```python
b = (i ** 4 for i in range(0,4))
print(b)
print("first")
for a in b:
    print(a)
print("second")
for i in b:
    print(i)
    
```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_10/lab_11/4.jpg)
 
## Выводы

В отличии от генератора списка такой генератор не возможно вывести для взгляда пользователя просто вызвав его имя через принт, для его проявления потребуется добавить итератор. Заметил странный момент если попробовать итерировать с помощью `iter()` то ничего не выйдет мы не поличим числа в консоли, получим тоже что и при попытке вывести сам генератор. получается с помощью `iter()` можно итерировать только списки. Так же мы узнали что из-за особеннсти генераторов, они не выводятся больше одного раза.

## Лабораторная работа №5

### Такой же счетчик, как и в первом задании, только это генератор и использует `yield`

```python
def count(con):
    while con <= 100:
        yield con
        con += 1


counter = count(10)
for i in counter:
    print(i)


```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_10/lab_11/5.jpg)

## Выводы

В данном коде мы видим как сильно ускоряют процесс генератор `yield` как я понял он использует последнее число. 

## Самостоятельная работа №1

### Вас никак не могут оставить числа Фибоначчи, очень уж они вас заинтересовали. Изучив новые возможности Python вы решили реализовать программу, которая считает числа Фибоначчи при помощи итераторов. Расчет начинается с чисел 1 и 1. Создайте функцию fib(n), генерирующую n чисел Фибоначчи с минимальными затратами ресурсов. Для реализации этой функции потребуется обратиться к инструкции yield (Она не сохраняет в оперативной памяти огромную последовательность, а дает возможность "доставать" промежуточные результаты по одному). Результатом решения задачи будет листинг кода и вывод в консоль с числом Фибоначчи от 200.

```python
def fib(n):
    a = 1
    b = 1

    for _ in range(n):
        yield a
        c = a
        a = b
        b = c + b


fibon = fib(1000)
for num in fibon:
    print(num)

```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_10/tema_11/1.jpg)

## Листинг

Этот код определяет функцию `fib()`, которая генерирует числа Фибоначчи с помощью генератора. Затем создается объект `fibon`, который является результатом вызова функции `fib(1000)`. Затем происходит итерация по объекту `fibon` с помощью цикла `for`, и каждое число печатается с помощью функции `print()`.

## Вывод

В данном коде мы видим как сильно ускоряют процесс генератор `yield` как я понял он использует последнее число и в моём случае получается мы складываем всегда только 2 числа и в памяти не держим всё остольное, как итог програма всего за пару секунд способна вычислять 1000 значение фибоначи. 

## Самостоятельная работа №2

###  К коду предыдущей задачи добавьте запоминание каждого числа Фибоначчи в файл "fib.txt", при этом каждое число должно находиться на отдельной строчке. Результатом выполнения задачи будет листинг кода и скриншот получившегося файла.

```python
def fib(n):
    a = 1
    b = 1

    for _ in range(n):
        yield a
        c = a
        a = b
        b = c + b


fibon = fib(10)
for num in fibon:
    with open("fib.txt", "a") as f:
        f.write(str(num))
        f.write("\n")
    print("--", num)

```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_10/tema_11/2.jpg)

## Листинг

Этот код определяет функцию `fib()`, которая генерирует числа Фибоначчи с помощью генератора. Затем создается объект `fibon`, который является результатом вызова функции `fib(10)`. Затем происходит итерация по объекту `fibon` с помощью цикла `for`. В каждой итерации число печатается с помощью функции `print()`, и затем число записывается в файл `"fib.txt"` с помощью оператора `with open("fib.txt", "a") as f:`. Функция `write()` используется для записи числа в файл, а `write("\n")` добавляет новую строку после числа.

## общий Вывод по теме

В общем мы изучили тему генераторов и итераторов, первое что мы узнали так это то что итерация значсит перебор, в первой лабе мы увидили как именно работает итератор. Узнали разницу между итератором через цикл `for` и `iter()`. Узнали как можно делать гибкие настройки для итератора. так же узнали что можно выводить отдельно итерируемые элементы по очереди с помощью `next()`. так же узнали что из за особености генераторов итерируемые числа выводятся всего раз. Узнали как работает `yield`. 
