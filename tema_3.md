# Тема 3. Операторы, условия, циклы
Отчет по Теме #3 выполнил:
- Боровягин Данил Андреевич
- ПИЭ-21-2

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | + |
| Задание 6 | + | + |
| Задание 7 | + | + |
| Задание 8 | + | + |
| Задание 9 | + | + |
| Задание 10 | + | + |

Работу проверили:
- к.э.н., доцент Панов М.А.

## Лабораторная работа №1

### Создайте две переменные, значение которых будете вводить через консоль. Также составьте условие, в котором созданные ранее переменные будут сравниваться, если условие выполняется, то выведете в консоль «Выполняется», если нет, то «Не выполняется».

```python
one = int(input("первое число для сравнения"))
two = int(input("второе число для сравнения"))

if one > two:
    print("выполняется")
else:
    print("не выполняется")
```
### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_3/lab_3/1.jpg)

## Выводы

В представленном коде мы рассмотрели как работает `if` и `else` простым языком `if` это делай если, а `else` иначе.
У нас если первая переменная больше второй то делай выполняется иначе не выполняется.

## Лабораторная работа №2

### Напишите программу, которая будет определять значения переменной меньше 0, больше 0 и меньше 10 или больше 10. Это нужно реализовать при помощи одной переменной, значение которой будет вводится через консоль, а также при помощи конструкций if, elif, else.

## Первый вариант решиня

```python
one = int(input("введите число"))

if one < 0:
    print("число меньше 0")

else:
    if 0 < one < 10:
        print("число больше 0 и меньше 10")

    else:
        if one > 10:
            print("число больше 10")
        else:
            print("число равно 10")
```


### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_3/lab_3/2.1.jpg)

## Второй вариант решения 

```python
one = int(input("введите число"))

if one < 0:
    print("число меньше 0")

elif 0 < one < 10:
    print("число больше 0 и меньше 10")

elif one > 10:
    print("число больше 10")
else:
    print("число равно 10")
```


### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_3/lab_3/2.2.jpg)


## Выводы

по сути `elif` нужен лишь для упрощения кода и ускорения его написания что бы не создавать цикл `if` каждый раз. Это мы поняли из данных кодов.

## Лабораторная работа №3

### Напишите программу, в которой будет проверяться есть ли переменная в указанном массиве используя логический оператор in. Самостоятельно посмотрите, как работает программа со значениями которых нет в массиве numbers.


```python
massiv = [4,44,444,4444]
one = int(input("введите число для поиска в массиве"))

if one in massiv:
    print("число обнаруженно")

else:
    print("число не обнаруженно")
```

### Результат 1.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_3/lab_3/3.1.jpg)

### Результат 2.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_3/lab_3/3.2.jpg)

## Выводы

в данной программе мы видм пример рабыты с массивом, на питоне он выглядит проще чем в других языках на которых я пробовал писать. всё предельно просто с помощью `if` мы проверяем условиме нахождения числа в массиве всего одним оператором `in`. если число в массиве.


## Лабораторная работа №4

### Напишите программу, которая будет определять находится ли
переменная в указанном массиве и если да, то проверьте четная она или нет. Самостоятельно протестируйте данную программу с разными значениями переменной value.

```python
massiv = [-4, -5, 2, 4, 6, 8, 10]

one = int(input("Введите число: "))

if (one in massiv):
    print("Число находится в массиве")

    if one < 0:
        print("Число отрицательное")

    elif one > 0:
        print("число положительное")

    if one % 2 == 0:
        print("Число является четным")

    else:
        print("Число не является четным")

else:
    print(F"числа {one} нет в массиве")

```
### Результат 1.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_3/lab_3/4.1.jpg)

### Результат 2.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_3/lab_3/4.2.jpg)

### Результат 3.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_3/lab_3/4.3.jpg)

### Результат 4.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_3/lab_3/4.4.jpg)

 
## Выводы

И так в данном коде я попытался применить `elif` что бы наглядно понять как и когда его применять, как я понял его стоит применять тогда когда через обычное добавление ещё одного цикла иф не получится достичь жаемого результата, так в моём случае я сделал доп проверку на положительность и отрицательность числе простым циклом не возможно сделать проверку на оба критария при этом продолжая проверку остальных критериев посли. затем я проверил как работает програма с 4 разными числами такими как `-5, -3, 4, 3`


## Лабораторная работа №5

### Напишите программу, в которой циклом for значения переменной і будут меняться от 0 до 10 и посмотрите, как разные виды сравнений и операций работают в цикле.

```python
for i in range(10):
    print(i)
    if i == 0:
        i += 2
    if i == 1:
        continue
    if i == 2 or i == 3:
        print("переменная равна 2 или 3")
    elif i in [4,6,5]:
        print("переменная равна 4,5,6")
    else:
        break
```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_3/lab_3/5.jpg)

## Выводы

Очень странный цикл с одной строны он полностью понятный но если додумывать что и как можно сделать на его основе то очень много нюансов. и так цикл `for` даёт нам повторение одного действия или одного элемента кода `n` раз вместе с ним есть такие операторы как `break` - прерывает выполнение повторений нашего цикла, `continue` - прерывает текущую энтерацию и переходит к следующей 

## Лабораторная работа №6


### Напишите программу, в которой при помощи цикла for определяется есть ли переменная value в строке string и посмотрите, как работает оператор else для циклов. Самостоятельно посмотрите, что выведет программа, если значение переменной value оказалось в строке string. Определять индекс буквы не обязательно, но если вы хотите, то это делается при помощи строки: index = string. find(value)Вы берете название переменной, в которой вы хотите что-то найти, затем применяете встроенный метод find() и в нем указываете то, что вам нужно найти. Данная строка вернет индекс искомого объекта.

```python
string = "Счастливых голодных игр"
value = input("введите букву для поиска")
for i in string:
    if i == value:
        index = string.find(value)
        print(f"буква {value} найдена в индексе {index}")
        break
else:
    print(f"буква {value} не найдена")
```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_3/lab_3/6.jpg)

## Выводы

В данном коде мы при помощи метода `.find()` определили есть ли буква в строке `sring` это же можно проделать и без метода  `.find()` используя только `for` и `if` метод `.find()` всегда возвращает индекс первого вхождения указанного значения. Важно помнить что операторы `if` и `else` нужно располагать строго относитьльно друг друга иначе ничего не будет срабатывать!

## Лабораторная работа №7

### Напишите программу, в которой вы наглядно посмотрите, как работает цикл for проходя в обратном порядке, то есть, к примеру не от 0 до 10, а от 10 до 0. В уже готовой программе показано вычитание из 100, а вам во время реализации программы будет необходимо придумать свой вариант применения обратного цикла.

```python
val = 100
for i in range(10, -1, -2):
    val -= i
    print(i,val)
```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_3/lab_3/7.jpg)

## Выводы

В данном коде мы отсчитываем в обратном порядке от 10 до 0 первое значение в выражении `range(10, -1, -1)` это то с чего начинается отсчёт для цикла `for` вторая цифра это до каких пор будет выполнятся цикл `for` и третья цифра это шаг в нашём случае будет уменьшатся на -1.


## Лабораторная работа №8

### Напишите программу используя цикл while, внутри которого есть какие-либо проверки, но быть осторожным, поскольку циклы while при неправильно написанных условиях могут становится бесконечными, как указано в примере далее.

```python
val = 0
while val < 100:
    if val == 0:
        val += 10
    elif val // 5 >= 2:
        val *= 5
    else:
        val -= 5
    print(val)
```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_3/lab_3/8.jpg)

## Выводы

В данном коде мы изучили цикл `while` обычно он звучит как делай до тех пор пока и условие. Тут так и есть делай до тех пор пока `val` меньше 100. Чтобы цикл не был бесконечным как в примере я добавил одно маленькое изменение в этой строчке `elif val // 5 >= 2:`. Вцелом я понял как работает цилк `while`	

## Лабораторная работа №9

### 

```python
val = 0
for i in range(3):
    for j in range(3):
        if i != j:
             val += j
        else:
            pass

print(val)
```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_3/lab_3/9.jpg)

## Выводы

цикл `for` в цикле `for` работает следующим образом сначала вверхнем цикле выбирается первое число у нас 0 затем во втром тоже 0 первая итерация 
в первом ещё 0 во втором уже 1 втрорая итераци 
в первом ещё 0 во втром уже 2 третья этерацияя 
у нас есть ещё условия что числа не равны оно тоже проверяется в этих этерациях.

## Лабораторная работа №10

### Напишите программу с использованием flag, которое будет определять есть ли нечетное число в массиве. В данной задаче flag выступает в роли индикатора встречи нечетного числа в исходном массиве, четных чисел.


```python
massiv = [7, 43, 433, 4443, 41]
flag = False
for i in massiv:
    if i % 2 == 0:
        flag = True

if flag is True:
    print("есть чётное число")
else:
    print("нет чётного числа ")
```

### Результат 1.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_3/lab_3/10.1.jpg)

### Результат 2.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_3/lab_3/10.2.jpg)


## Вывод

На основе представленного кода можно сделать вывод что при помощи задания переменной логического значения `true` или `false` можно определелять итоговый вывод. 

## Лабораторная работа №10

### увидел лишний скриншот решил посмотреть как работает эта программа 


```python
string = "нет в массиве"
val = [2, 5, 6, 8, 11]
con = 0
while "есть в массиве" not in string:
    memo = string
    if con in val:
        string = "есть в массиве"
    print(string)
    if con < 10:
        string = memo
    con += 1
```

### Результат 1.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_3/lab_3/11.jpg)

## Вывод

с помощью данной программы при доработке можно сделать поисковик по массиву что бы он прописывал каждое число какое есть в массиве и которого нет. Узнал что есть места где можно не использовать `else`.

## Самостоятельная работа №1

### Выведите в консоль булевую переменную False, не используя слово False в строке или изначально присвоенную булевую переменную. Программа должна занимать не более двух строк редактора кода.

```peyhon
one = ("")
print(bool(one))
```
нашёл 2 варианта решения 

```python
one = "настя"
print(not bool(one))
```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_2/tema_2/1.jpg)

## Вывод

из двух данных строчек кода можно понять что для выведения булевой переменной со значением `false` можно использовать как пустое значание оно всегда будет равно false либо 0, либо использовать отрицание которое поменяет значение true на false.


## Самостоятельная работа №2


### Присвоить значения трем переменным и вывести их в консоль, используя только две строки редактора кода

```python
x,y,z = 1,2,3
print(x,y,z)
```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_2/tema_2/2.jpg)

## Вывод

можно присваивать переменные соответственно их порядковому номеру, а именно если x стоит первая то и число пресвоиное её должно стоять первым y стоит втрой её значение стоит вторым по счёту после равно. `print()` без проблем выводит числа по порядку переменных (важно можно прописывать каждую букву отдельно тогда `print()` выведет её значение на консоль 


## Самостоятельная работа №3

### Реализуйте ввод данных в программу, через консоль, в виде только целых чисел (тип данных int). Tо есть при вводе буквенных символов в консоль, программа не должна работать. Программа должна занимать не более двух строк редактора кода.

```python
one=input("целое число:")
print(int(one))
```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_2/tema_2/3.1.jpg)

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_2/tema_2/3.2.jpg)

## Вывод

в этом коде мы реализовали ввод данных пользователем который при вводе чисел формата `не int` прекращает работу с ошибкой. данны код можно доработать и выводить на экран сообщение о том что программа прекратила рабоботу из-за не правильного значения числа. 

## Самостоятельная работа №4

### Создайте только одну строковую переменную. Длина строки должна не превышать 5 символов. На выходе мы должны получить строку длиной не менее 16 символов. Программа должна занимать не более двух строк редактора кода.

```python
one = "0123"
print(one * 4)
```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_2/tema_2/4.jpg)

## Вывод

в этом коде мы применили то что нам было представлено в лаб работе, присвоели переменной слово длинной в 4 знака затем при выводе с помощью `print()` умножили на 4 и того получили 16 знаков. 

## Самостоятельная работа №5

### Создайте три переменные: день (тип данных - числовой), месяц (тип данных - строка), год (тип данных - числовой) и выведите в консоль текущую дату в формате: "Сегодня день месяц год. Всего хорошего!" используя F строку и оператор end внутри print(), в котором вы должны написать фразу "Всего хорошего!". Программа должна занимать не более двух строк редактора кода.

```python
day,month,year = 19,"Сентября",2023
print(f"Сегодня {day,month,year}", end = " Всего хорошего!")
```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_2/tema_2/5.jpg)

## Вывод

в этом коде мы при помощи трёх переменных в одном ряду прописываем день месяц и год затем с помощью `print()` выводим на экран консоли Сегодня (19, 'Сентября', 2023) Всего хорошего! делается это спомощью f строки. Также используем `end-` для того что бы всё предложение писалось в строчку без переносов. так как по дефолту в пайтоне идёт перенос строки через `\n`

## Самостоятельная работа №6

### В предложении 'Hello World' вставьте 'my' между двумя словами. Выведите полученное предложение в консоль в одну строку. Программа должна занимать не более двух строк редактора кода.

```python
md = "my"
print(f"hello {md} world")
```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_2/tema_2/6.jpg)

## Вывод

В данном коде я попробовал реализовать один из навыков полученых при прохождении лабораторных работ, а именно при помощи строки`f` которая позволяет нам вставлять переменные в текст вывода. добавил `my` 

## Самостоятельная работа №7

### Узнайте длину предложения 'Hello World', результат выведите в консоль. Программа должна занимать не более двух строк редактора кода.

```python
vor="Hello World"
print(len(vor))
```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_2/tema_2/7.jpg)

## Вывод

В представленном коде мы посчитали длинну объекта при помощи функции `len()` так же можно посчитать кол - во элементов. 


## Самостоятельная работа №8

### Переведите предложение 'HELLO WORLD' в нижний регистр. Программа должна занимать не более двух строк редактора кода.

```python
vor="HELLO WORLD"
print(vor.lower())
```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_2/tema_2/8.jpg)

## Вывод

в данном коде мы уменьшили регистр с помомщью метода `.lower()` так же есть обратный метод который повышает регистр `.upper` ещё есть метод для преоброзование только первого символа в верхний регистр и понижения всех остальных `.capitalize`.



## Самостоятельная работа №9

### Напишите программу, которая запрашивает у пользователя два числа и выводит сумму их квадратов.

```python
num1 = float(input("Введите первое число: "))
num2 = float(input("Введите второе число: "))

sum_of_squares = num1**2 + num2**2

print("Сумма квадратов чисел:", sum_of_squares)

```
### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_2/tema_2/9.jpg)

## Вывод

я написал простую задачу, по пройденому материалу, на уменее работать с математическими выражениями в `python`. В данном коде сначала мы создаём две переменные для ручного вода чисел, используем `float` для преобразования строки в числа. Затем выполняем простое возведение в квадрат каждого и складываем их, В конце выводим результат через `print()`.

## Самостоятельная работа №10

### вывести на экран слово введённое пользователем изменить его регистр(первая заглавная остальные маленькие) и посчитать кол-во символов. Программа должна занимать не более 6 строк кода


```python
word=input("Ведите слово для обработки:")
print("вы ввели слово:" + word)
cap_word=word.capitalize()
print("регистр был изменён:" + cap_word)
len_word=len(word)
print("кол-во символов:" + str(len_word))
```
### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_2/tema_2/10.jpg)

## Вывод

в данной задаче объеденили несколько операций проделавыемых с введённым словом. дляначала воодится слово затем при помощи `input()` оно выводит на консоль после чего изменяется его регистр с помощью метода `.capitalize()` и последнее считаеттся длинна вводимого слова при помощи функции `len()` всё это выводится на экран

## общий Вывод по теме

Все лабораторные работы позволяют ознакомиться с различными аспектами программирования на языке Python, такими как вывод в консоль, математические операции, работа со строками и преобразование типов переменных. Эти навыки являются фундаментальными для дальнейшего изучения и применения языка программирования Python. так что вообщем мы ознакомились с базовыми навыками которые так не обходимы для дальнейшего изучения