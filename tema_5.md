# Тема 4. Функции и модули
Отчет по Теме #4 выполнил:
- Боровягин Данил Андреевич
- ПИЭ-21-2

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | +- |
| Задание 6 | + |   |
| Задание 7 | + |   |
| Задание 8 | + |   |
| Задание 9 | + |   |
| Задание 10 | + |   |

Работу проверили:
- к.э.н., доцент Панов М.А.

## Лабораторная работа №1

### Друзья предложили вам поиграть в игру "найди отличия и убери повторения (версия для программистов)". Суть игры состоит в том, что на вход программы поступает два множества, а ваша задача вывести все элементы первого, которых нет во втором. А вы как раз недавно прошли множества и знаете их возможности, поэтому это не составит для вас труда. 
P.S. Посмотрите что происходит с повторяющимися значениями в
множествах, это достаточно интересно.

```python
set_1 = {"lol", "dot","drag", "dank", "dot"}
set_2 = {"lol","gree","dota","agly",}

print(set_2 - set_1)
print(set_1 - set_2)

```
### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_5/lab_5/1.jpg)

## Выводы

В данном коде мы сравниваем 2 набора чего либо, програма сего навсего выдаёт те значения которые не повторяются с тем набром с которым идёт сравнение так можно сравнивать большое количество данных и получать список тех данных которые не повторяются. 

## Лабораторная работа №2

### Напишите две одинаковые программы, только одна будет использовать set(), а вторая frozenset() и попробуйте к исходному множеству добавить несколько элементов, например, через цикл.

## Первый вариант решиня

```python
a = set("asdfgh")
print(a)
for i in range(5):
    a.add(i)
print(a)

b = frozenset("asdfgh")
print(b)
for s in range(5):
    b.add(s)
print(b)
```


### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_5/lab_5/2.jpg)

## Выводы

В данном коде мы увидили наглядно разницу между `set` и `frozenset` в то время как `set` может изменятся и примать новые значения, `frozenset` остаётся не изменным и программа просто выдаст ошибку.

## Лабораторная работа №3

### На вход в программу поступает список (минимальной длиной 2 символа). Напишите программу, которая будет менять первый и последний элемент списка.

```python
def rep(input_list):
    mem = input_list[1]
    input_list[1] = input_list[3]
    input_list[3] = mem

    return input_list


print(rep([5,4,3,2,1]))


def swap(lst):
    lst[0], lst[-1] = lst[-1], lst[0]
    return lst


print(swap([1,2,3,4,5]))
```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_5/lab_5/3.jpg)

## Выводы

В данном коде мы рассмотрели как можно поменять местами 2 любых символа, так же я нашёл вариант более простого решения где используется всего пара строчек для тойже операции благодаря свойствам `python`.

## Лабораторная работа №4

### На вход в программу поступает список (минимальной длиной 10 символов). Напишите программу, которая выводит элементы с индексами от 2 до 6. В программе необходимо использовать "срез".

```python
a = [12, 54, 32, 57, 843, 2346, 765, 75, 25, 234, 756, 23]
print(a[4:])

```
### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_5/lab_5/4.jpg)
 
## Выводы

В данном коде мы расмотрели как получить срез данных из массива, обратившись просто к определённому элементу по индексу. Если не указать первое значение то отсчёт по дефолту начнётся с первого элемента, если не указать последнее втрое значение то срез продлится до конца элементов вплоть до самого последнего 

## Лабораторная работа №5

### Иван задумался о поиске «бесполезного» числа, полученного из списка. Суть поиска в следующем: он берет произвольный список чисел, находит самое большое из них, а затем делит его на длину списка. Студент пока не придумал, где может пригодиться подобное
значение, но ищет у вас помощи в реализации такой функции useless().

```python
def useless(numbers):
    if not numbers:
        return None

    max_number = max(numbers)
    length = len(numbers)
    useless_value = max_number / length

    return useless_value


execution_times = [1, 50, 10, 1, 15, 3]


useless_num = useless(execution_times)

print("Бесполезное число:", useless_num)


for index, time in enumerate(execution_times):
    if time > useless_num:
        print("Шаг", index+1, "имеет продолжительность выше среднего")
    else:
        print("Шаг", index+1, "имеет продолжительность ниже или равную среднему")

```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_5/lab_5/5.jpg)

## Выводы

В данном коде я попытался представить как можно применить бесполезное число

## Лабораторная работа №6


### Ребята не могут определится каким супергероем они хотят стать. У них есть случайно составленный список супергероев, и вы должны определить кто из ребят будет каким супергероем. Необходимо использовать разделение списков.

```python
hero = ["супермен", "человек-паук", "бэтмен"]

nikolai, fedya, danil = hero

print("николай: ",nikolai)
print("федя: ",fedya)
print("данил: ",danil)
```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_5/lab_5/6.jpg)

## Выводы

В данном коде я не совсем понял чему мы должны были научится, но мы сделали слудующее задали переменно `hero` список и затем создали ещё 3 переменные которые приравняли к первой, суть просто из первой переменной списком мы попорядку присваим значения в те переменные которые создали ниже. каждая переменная будет сообветствовать индексу из списка. так николай будет спермен федя человек паук и тд. 

## Лабораторная работа №7

### Вовочка, насмотревшись передачи "Слабое звено" решил написать программу, которая также будет находить самое слабое звено (минимальный элемент) и удалять его, только делать он это хочет не с людьми, а со списком. Помогите Вовочке с реализацией программы. Подсказка: для этого вам необходимо отсортировать список и удалить значение при помощи рор().

```python
a = [-4, -44,12,23,-53,56,61,-57,44,41,4,-3,-7]
a.sort()
print("отсортированный список\n", a)
a.pop(-1)
print("отсортированный список беза наименьшего элемента\n", a)
```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_5/lab_5/7.jpg)

## Выводы

в данном коде мы увидили как работет `sort` и `pop` сортировать можно и более сложно например можно добавить свои парметры такие как сортировать не обращая внимания на регистр например или сортировать в обратном порядке. `pop` удаляет исключить по индексу указать более одного индекса не возможно можно укзать переменную которая будет содержать индекс для удаления. например мы всчитаем значение для переменной а потом вмето числа возле `pop` просто подставим эту переменную.
  
## Лабораторная работа №8

### 

```python
from random import *


def list():
    f = [randint(4,444)] * randint(4,44)
    return f


res = []
for i in range(randint(1,4)):
    res.append(list())

    print(res)

```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_5/lab_5/8.jpg)

## Выводы

в данной программе мы увидили как можно постороить списки наполненные совершенно случайными числами, применений этому очень много. суть данно кода такова в функции `list` мы задаём длинну списка и значения которые будут в списке, затем в цикле `for` мы мы задаём случайное число итераций, каждая итерация будет сосздавать новый списоки при помощи `apened` будет добавлять его в конец переменной `res` если убрать `apened` то переменная рес выдаст значения последней итерации.

## Лабораторная работа №9

### Вы работаете в ресторане и отвечает за статистику покупок, ваша задача сравнить между собой заказы покупателей, которые указаны в разном порядке. Реализуйте функцию superset(), которая принимает 2 множества. Результат работы функции: вывод в консоль одного из сообщений в зависимости от ситуации:
«Супермножество не обнаружено»
«Объект {X} является чистым супермножеством»
«Множества равны»

```python
def superset(set_1, set_2):
    if set_1>set_2:
        print(f"обект {set_1} является чистым супер множеством")
    elif set_1==set_2:
        print("множества равны")
    elif set_1<set_2:
        print(f"обект {set_2} является чистым супер множеством")
    else:
        print("супермножеств не обнаружено")

superset({1,2,6,4,5,3}, {2,3,1})
superset({2,3,1},{1,2,3,4,5,6})
superset({2,3,1},{1,2,3})
superset({2,3,1,7},{1,2,3,4,5,6})
```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_5/lab_5/9.jpg)

## Выводы

В данном коде мы видимо как сравниваются `set_1` и `set_2` происходит это следующим образом если одна из переменных полность состоит из числе которые входят во вторую переменную то это будет супер множество на экран выведется та переменная которя длинее, если переменные имеют полностью идентичный набор чисел, то они равны

## Лабораторная работа №10

### Предположим, что вам нужно разобрать стопку бумаг, но нужно начать работу с нижней, "переверните стопку". Вам дан произвольный список. Представьте его в обратном порядке. Программа должна занимать не более двух строк в редакторе кода.

```python
list = [5,3,7,1,5]
print(list[::-1])
```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_5/lab_5/10.jpg)

## Вывод

В данном коде на основе среза мы делаем обратный список, `(start:stop:step)` где `start` и `stop` будут дефолтными нальный индекс 0, а конечный равен длиннне списка. шаг -1 выведет список в обратном порядке  

## Самостоятельная работа №1

### Ресторан на предприятии ведет учет посещений за неделю при помощи кода работника. У них есть список со всеми посещениями за неделю. Ваша задача почитать:
Сколько было выдано чеков
Сколько разных людей посетило ресторан
Какой работник посетил ресторан больше всех раз

```peyhon
from collections import Counter

# Определяем список выданных чеков за неделю
chek = [8734, 2345, 8201, 6621, 9999, 1234, 5678, 8201, 8888, 4321, 3365,
        1478, 9865, 5555, 7777, 9998, 1111, 2222, 3333, 4444, 5556, 6666,
        5410, 7778, 8889, 4445, 1439, 9604, 8201, 3365, 7502, 3016, 4928,
        5837, 8201, 2643, 5017, 9682, 8530, 3250, 7193, 9051, 4506, 1987,
        3365, 5410, 7168, 7777, 9865, 5678, 8201, 4445, 3016, 4506, 4506]

# Вычисляем количество выданных чеков
individ = len(chek)
print("Количество выданных чеков за неделю:", individ)

# Вычисляем количество разных посетителей
client = len(set(chek))
print("Количество разных посетителей:", client)

# Создаем объект Counter для подсчета посещений каждого работника
con = Counter(chek)

# Находим наиболее часто встречающегося работника и его количество посещений
most_1 = con.most_common(1)

most = most_1[0][0]
count = most_1[0][1]

# Выводим результаты на экран
print(f"Работник номер: {most} посетил ресторан {count} раз")

```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_5/tema_5/1.jpg)

## Вывод
 
В этом коде мы сначала вычисляем общее количество выданных чеков individ с помощью функции len(). Затем мы вычисляем количество разных посетителей client, используя множество.
Затем мы создаем объект Counter из списка chek, чтобы подсчитать количество посещений каждого работника. Далее, мы используем метод most_common() для получения наиболее часто встречающегося работника и его количество посещений.
Наконец, мы выводим результаты на экран, включая номер работника, который посетил ресторан наибольшее количество раз.

## Самостоятельная работа №2


###  На физкультуре студенты сдавали бег, у преподавателя физкультуры есть список всех результатов, ему нужно узнать
Три лучшие результата
Три худшие результата
Все результаты начиная с 10 
Ваша задача помочь ему в этом.


```python
results = [10.2, 14.8, 19.3, 22.7, 12.5, 33.1, 38.9, 21.6, 26.4, 17.1, 30.2, 35.7, 16.9, 27.8, 24.5, 16.3, 18.7, 31.9, 12.9, 37.4]

# Сортировка результатов по возрастанию
sorted_results = sorted(results, reverse=False)

# Вывод трех лучших результатов
top_three_results = sorted_results[:3]
print("Три лучших результата:")
for result in top_three_results:
    print(result)

# Сортировка результатов по убыванию
sorted_results = sorted(results, reverse=True)

# Вывод трех худших результатов
top_three_results = sorted_results[:3]
print("Три худших результата:")
for res in top_three_results:
    print(res)

# Сортировка результатов по возрастанию
sorted_results = sorted(results, reverse=False)

# Вывод всех результатов начиная с 10
print("Все результаты начиная с 10:")
for all in sorted_results:
    print(all)
```


```python
results = [10.2, 14.8, 19.3, 22.7, 12.5, 33.1, 38.9, 21.6, 26.4, 17.1, 30.2, 35.7, 16.9, 27.8, 24.5, 16.3, 18.7, 31.9, 12.9, 37.4]

# Сортировка результатов по возрастанию
results.sort()
top_3 = results[0:3]
print("3 лучших результата\n", top_3)

results.sort(reverse=True)
bad_3 = results[0:3]
print("3 худших результата\n", bad_3)

results.sort()
print("все результаты начиная с 10\n ", results)
```

### Результат 1.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_5/tema_5/2.jpg)

### Результат 2.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_5/tema_5/2.1.jpg)


## Вывод

В данном коде мы сортируем числа при помощи встроеной функции в `python` `sorted` она сортирует числа в зависимости от параметра `reverse` если он `True` то от большего к меньшему если `false` то от меньшего к большему. затем при помощи цикла `for` мы получаем 3 наших значения либо все наши значения начиная с 10. Второй код был написан после выполнения всех лабараторных работ а первый до их выполнения. потому решил оставить оба варианта.

## Самостоятельная работа №3

### Преподаватель по математике придумал странную задачку. У вас есть три списка с элементами, каждый элемент которых - длина стороны треугольника, ваша задача найти площади двух треугольников, составленные из максимальных и минимальных элементов полученных списков. Результатом выполнения задачи будет: листинг кода, и вывод в консоль, в котором будут указаны два этих значения.
Три списка:
one = [12, 25, 3, 48, 71]
two = [5, 18, 40, 62, 98]
three = [4, 21, 37, 56, 84]

```python
from math import *

one = [12, 25, 3, 48, 71]
two = [5, 18, 40, 62, 98]
three = [4, 21, 37, 56, 84]

#минимальные значения сторон

one.sort()
two.sort()
three.sort()

a = one[0]
b = two[0]
c = three[0]

#функция подсчёта площади через формулу герона


def tria(a,b,c):
    s = (a + b + c) / 2
    S = sqrt(s * (s - a) * (s - b) * (s - c))
    return S


print(f"сторона а {a} сторона b {b} сторона c {c}")
print("площадь наименьшего треугольника: ",tria(a, b, c))

# Наибольшие значения сторон
one.sort(reverse=True)
two.sort(reverse=True)
three.sort(reverse=True)

a = one[0]
b = two[0]
c = three[0]

print(f"сторона а {a} сторона b {b} сторона c {c}")
print("площадь наибольшего треугольника: ", tria(a, b, c))
```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_5/tema_5/3.jpg)

## Вывод

В данной программе при помощи функции `sort` мы сортируем числа от меньше к больше и наобарот затем просто по индексу выбираем первое число оно же и будет наименьшим или наибольшим после сортировки. пишем функцию для подсчёта площади треугольника по трём сторона в нашем случае это формула герона и выводим результат

## Самостоятельная работа №4

### Никто не любит получать плохие оценки, поэтому Борис решил это исправить. Допустим, что все оценки студента за семестр хранятся в одном списке. Ваша задача удалить из этого списка все двойки, а все тройки заменить на четверки.
Списки оценок (проверить работу программы на всех трех вариантах):
[2, 3, 4, 5, 3, 4, 5, 2, 2, 5, 3, 4, 3, 5, 4]
[4, 2, 3, 5, 3, 5, 4, 2, 2, 5, 4, 3, 5, 3, 4] 
[5, 4, 3, 3, 4, 3, 3, 5, 5, 3, 3, 3, 3, 4, 4]
Результатом выполнения задачи будет: листинг кода, и вывод в консоль, в котором будут три обновленных массива.

```python
one = [2, 3, 4, 5, 3, 4, 5, 2, 2, 5, 3, 4, 3, 5, 4]
two = [4, 2, 3, 5, 3, 5, 4, 2, 2, 5, 4, 3, 5, 3, 4]
three = [5, 4, 3, 3, 4, 3, 3, 5, 5, 3, 3, 3, 3, 4, 4]
four = 4
vi = [2]

updete_1 = [four if i == 3 else i for i in one]
updete_2 = [four if i == 3 else i for i in two]
updete_3 = [four if i == 3 else i for i in three]

for d in vi:
    while d in updete_1:
        updete_1.remove(d)

for d in vi:
    while d in updete_2:
        updete_2.remove(d)

for d in vi:
    while d in updete_3:
        updete_3.remove(d)
print(updete_1)
print(updete_2)
print(updete_3)
```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/тема_5/tema_5/4.jpg)

## Вывод



## Самостоятельная работа №5

### Вам предоставлены списки натуральных чисел, из них необходимо сформировать множества. При этом следует соблюдать это правило: если какое-либо число повторяется, то преобразовать его в строку по следующему образцу: например, если число 4 повторяется 3 раза, то в множестве будет следующая запись: само число 4, строка «44», строка «444».
Множества для теста:
list_1 = [1, 1, 3, 3, 1]
list_2 = [5, 5, 5, 5, 5, 5, 5]
list 3 = [2, 2, 1, 2, 2, 5, 6, 7, 1, 3, 2, 2]
Результаты вывода (порядок может отличаться, поскольку мы работаем c set()):
{'11', 1, 3, '33', '111'}
{5, '5555', '5555555', '555555', '555', '55', '5555555'}
{'11', 1, 3, 2, 5, 6, '222222', '222', 7, '2222', '22222', '22'}

```python
list_1 = [1, 1, 3, 3, 1]
list_2 = [5, 5, 5, 5, 5, 5, 5]
list_3 = [2, 2, 1, 2, 2, 5, 6, 7, 1, 3, 2, 2]


def generate_combinations(num_list):
    combinations = set()
    for num in num_list:
        count = num_list.count(num)
        if count > 1:
            for i in range(2, count + 1):
                combinations.add(str(num) * i)
    return combinations


def transform_list(i):
    result = set()
    result.update(generate_combinations(i))
    result.update(set(i))
    return result


result_1 = transform_list(list_1)
result_2 = transform_list(list_2)
result_3 = transform_list(list_3)
print(f"{result_1}\n{result_2}\n{result_3}")
```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_5/tema_5/5.jpg)

## Вывод

Данный код понимаю с трудом, но вроде он работает если я всё правильно понимаю то мы просто, загружаем список в функцию `def transform_list(i): ` в качестве аргумента `i` затем создаём пустое множество и отправляем значение `i` в `def generate_combinations(num_list):` и  тут происходит проверка при помощи `count` мы считаем количество повторяющихся значений если они больше 1 то идём в цикл `for` где и преобразуем наш список.

## общий Вывод по теме

