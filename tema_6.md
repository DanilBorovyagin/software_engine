# Тема 6. Базовые коллекции: словари, кортежи
Отчет по Теме #6 выполнил:
- Боровягин Данил Андреевич
- ПИЭ-21-2

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | + |

Работу проверили:
- к.э.н., доцент Панов М.А.

## Лабораторная работа №1

### В школе, где вы учились, узнали, что вы крутой программист и попросили написать программу для учителей, которая будет при вводе кабинета писать для него ключ доступа и статус, занят кабинет или нет. При написании программы необходимо использовать словарь (dict), который на вход получает номер кабинета, а выводит необходимую информацию. Если кабинета, который вы ввели нет в словаре, то в консоль в виде значения ключа нужно вывести "None" и виде статуса вывести "False".
По большому счету написав данную программу мы с вами научились заменять иногда громоздкую конструкцию if/elif/else. Поскольку здесь функционал словаря полностью повторяет функционал условия, но при этом у использования словарей в более сложных программах есть намного больше возможностей реализации.

```python
req = int(input("введите номер кабинета "))

dic = {
    10: {"ключ": 101, "доступ": True},
    11: {"ключ": 102, "доступ": True},
    12: {"ключ": 103, "доступ": True},
    13: {"ключ": 104, "доступ": False},
    14: {"ключ": 105, "доступ": True},
    15: {"ключ": 106, "доступ": False},
    None:  {"ключ": None , "доступ": False},
}

res = dic.get(req)
if not res:
    res = dic[None]
k = res.get("ключ")
c = res.get("доступ")
print("ключ кабинета:",k, "Доступ к кабинету",c)
```
### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_6/lab_6/1.1.jpg)

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_6/lab_6/1.2.jpg)

## Выводы

В данном коде мы увидили как можно заменить огромную конструкцию из циклов `if` на небольшой код с картежем. Код работает следующим образом вводится число `get` передаёт эти данные в кортеж `dic` находится нужное значение если есть и передаётся в переменную `res` дальше цикл `if` в котором проверяется если `res` равно `None` то значение нон и будет выведено иначе мы выведем значение ключ и доступ. 

## Лабораторная работа №2

### Алексей решил создать самый большой словарь в мире. Для этого он придумал функцию dict maker (**kwargs), которая принимает неограниченное количество параметров «ключ: значение» и обновляет созданный им словарь my dict, состоящий всего из одного элемента «first» со значением «so easy». Помогите Алексею создать данную функцию.
Ниже на скриншоте мы использовали встроенный модуль pprint, который выводит большие объемы информации более понятно для восприятия человеческим глазом, Иногда очень удобно использовать данную возможность Python.

```python
from pprint import *

dic = {"first": "easy"}


def dict(**kwargs):
    dic.update(**kwargs)


dict(имя="Данил", возраст=21, цвет_глаз="зелёные" )
dict(x1=2, x3=44, y1=22, y2= "лол")
pprint(dic)
```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_6/lab_6/2.jpg)

## Выводы

В данном коде мы узнали как можно бесконечно добавлять новые значения в кортеж `kwargs` и изучили функцию `pprint` которая позволяет выводить большой объём данных, в более удобном для человека формате.

## Лабораторная работа №3

### Для решения некоторых задач бывает необходимо разложить строку на отдельные символы. Мы знаем что это можно сделать при помощи split(), у которого более гибкая настройка для разделения для этого, но если нам нужно посимвольно разделить строку без всяких условий, то для этого мы можем использовать кортежи (tuple). Для этого напишем любую строку, которую будем делить и "обвернем" ее в tuple и дальше мы можем как нам угодно с ней работать, например, сделать ее списком (тогда получится полный аналог split()) или же работать с ним дальше, как с кортежем.

```python
strin = "withyou"

res = tuple(strin)
print(res)
print(list(res))
```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_6/lab_6/3.jpg)

## Выводы

В данном коде мы рассмотрели как можно преобразовать, набор данных в кортеж или список. 

## Лабораторная работа №4

### Вовочка решил написать крутую функцию, которая будет писать имя, возраст и место работы, но при этом на вход этой функции будет поступать кортеж. Помогите Вовочке написать эту программу.

```python
def info (name, age, status, company="нет названия",):
    print(f"имя:{name} возвраст:{age} компания: {company} статус:{status} ")


dan = ("Данил", 21, "стажёр","google", )
info(*dan)

jin =("Андрей", 30, "работает","google", )
info(*jin)

jini =("Андрей", 30, "работает")
info(*jini)
```
### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_6/lab_6/4.jpg)
 
## Выводы

В данном коде мы инициализируем различные данные для нашей функции `info` и можем задать дефолтное значение при отсутсвии информации ниже. 

## Лабораторная работа №5

### Для сопровождения первых лиц государства Х нужен кортеж, но никто не может определиться с порядком машин, поэтому вам нужно написать функцию, которая будет сортировать кортеж, состоящий из целых чисел по возрастанию, и возвращает его. Если хотя бы один элемент не является целым числом, то функция возвращает исходный кортеж.

```python
list_2= (5,5,3,1,9)
list_1= (5,5,3,"1",9)
tup_2 = tuple(list_2)
tup_1 = tuple(list_1)

if not isinstance(tup_1, int):
    print(list_1)
else:
    print(sorted(tup_1))

if not isinstance(tup_2, int):
    print(list_2)
else:
    print(sorted(tup_2))

```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_6/lab_6/5.jpg)

## Выводы

В данном коде я посмотрел как написано у вас, и решил попробовать сделать по своему мой вариант не лучше вашего, скорее даже хуже, но мне было интересно посмотреть, как можно ещё реализовать сортировку кортежа и возврат его значений. Изучили новое свойство `isinstance` проверяет принадлежность объекта к классу или типу данных. 


## Самостоятельная работа №1

### При создании сайта у вас возникла потребность обрабатывать данные пользователя в странной форме, а потом переводить их в нужные вам форматы. Вы хотите принимать от пользователя последовательность чисел, разделенных пробелом, а после переформатировать эти данные в список и кортеж. Реализуйте вашу задумку. Для получения начальных данных используйте input(). Результатом программы будет выведенный список и кортеж из начальных данных.

```peyhon
user = input("Введите данные через пробел: ")

list = user.split()

tup = tuple(list)

print(list)
print(tup)
```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_6/tema_6/1.jpg)

## Вывод
 
В данном коде сначала мы преобразуем введённые данные в список, а затем в кортеж с помощью `tuple` 

## Самостоятельная работа №2

###  Николай знает, что кортежи являются неизменяемыми, но он очень упрямый и всегда хочет доказать, что он прав. Студент решил создать функцию, которая будет удалять первое появление определенного элемента из кортежа по значению и возвращать кортеж без него. Попробуйте повторить шедевр не признающего авторитеты начинающего программиста. Но учтите, что Николай не всегда уверен в наличии элемента в кортеже (в этом случае кортеж вернется функцией в исходном виде). Входные данные:(1, 2, 3), 1) (1, 2, 3, 1, 2, 3, 4, 5, 2, 3, 4, 2, 4, 2), 3) (2, 4, 6, 6, 4, 2), 9) Ожидаемый результат: (2, 3)(1, 2, 1, 2, 3, 4, 5, 2, 3, 4, 2, 4, 2) (2, 4, 6, 6, 4, 2)

```python
dat_1 = (1, 2, 3)
dat_2 = (1, 2, 3, 1, 2, 3, 4, 5, 2, 3, 4, 2, 4, 2)
dat_3 = (2, 4, 6, 6, 4, 2)


def delit(dat_mod, elm):
    dat_list = list(dat_mod)
    if elm in dat_list:
        inrem = dat_list.index(elm)
        del dat_list[inrem]
    dat_mod = tuple(dat_list)
    return dat_mod


dat_1 = delit(dat_1, 1)
dat_2 = delit(dat_2, 3)
dat_3 = delit(dat_3, 9)

print(dat_1)
print(dat_2)
print(dat_3)
```


```python
dat_1 = (1, 2, 3)
dat_2 = (1, 2, 3, 1, 2, 3, 4, 5, 2, 3, 4, 2, 4, 2)
dat_3 = (2, 4, 6, 6, 4, 2)

dat_list_1 = list(dat_1)
if 1 in dat_list_1:
    inrem = dat_list_1.index(1)
    del dat_list_1[inrem]
dat_1 = tuple(dat_list_1)

dat_list_2 = list(dat_2)
if 3 in dat_list_2:
    inrem = dat_list_2.index(3)
    del dat_list_2[inrem]
dat_2 = tuple(dat_list_2)

dat_list_3 = list(dat_3)
if 9 in dat_list_3:
    inrem = dat_list_3.index(9)
    del dat_list_3[inrem]
dat_3 = tuple(dat_list_3)

print(dat_1)
print(dat_2)
print(dat_3)
```


### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_6/tema_6/2.jpg)


## Вывод

Выше я предоставил 2 варианта кода, один был наброском но решил его сохранить для наглядности что у меня получилось сначала и к чему я пришёл в итоге. суть функции довльна просто она получает наш кортеж и элемент который необходимо удалить, далее из кортежа мы получем список, 

## Самостоятельная работа №3

### 

```python
from collections import Counter
user = input("число: ")


def rid(users):
    counter = Counter(users)
    most = counter.most_common(3)
    most_sort = sorted(most, key=lambda x: x[1])
    res = {int(num): count for num, count in most_sort}
    return res


user_most = rid(user)
print(user_most)
```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_6/tema_6/3.jpg)

## Вывод

В данной программе мы выводим 3 самых часто повторяемых числа и количество повторений. мы используем метод `Counter` для подсчёта, затем при помощи `most_common` мы выбираем 3 самых часто встречаемых числа. в конце мы создаём словарь `res` ключами в котором являются значения `most_sort`.

## Самостоятельная работа №4

### Ваш хороший друг владеет офисом со входом по электронным картам, ему нужно чтобы вы написали программу, которая показывала в каком порядке сотрудники входили и выходили из офиса. Определение сотрудника происходит по id. Напишите функцию, которая на вход принимает кортеж и случайный элемент (id), его можно придумать самостоятельно. Требуется вернуть новый кортеж, начинающийся с первого появления элемента в нем и заканчивающийся вторым его появлением включительно.
Если элемента нет вовсе - вернуть пустой кортеж.
Если элемент встречается только один раз, то вернуть кортеж, который начинается с него и идет до конца исходного.
Входные данные:
(1, 2, 3), 8)
(1, 8, 3, 4, 8, 8, 9, 2), 8)
(1, 2, 8, 5, 1, 2, 9), 8)
Ожидаемый результат:
(8, 3, 4, 8)
(8, 5, 1, 2, 9)

```python
def find(log_tuple, id):
    if id not in log_tuple:
        return ()
    start_index = log_tuple.index(id)

    try:
        end_index = log_tuple.index(id, start_index + 1)
    except ValueError:
        end_index = len(log_tuple)

    return log_tuple[start_index:end_index]


log_1 = (1, 2, 3)
id_1 = 8
result_1 = find(log_1, id_1)
print(result_1)

log_2 = (1, 8, 3, 4, 8, 8, 9, 2)
id_2 = 8
result_2 = find(log_2, id_2)
print(result_2)

log_3 = (1, 2, 8, 5, 1, 2, 9)
id_3 = 8
result_3 = find(log_3, id_3)
print(result_3)

```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_6/tema_6/4.jpg)


## Вывод

В данном коде мы создали функцию которая при наличии `id` в кортеже будет определять был ли сотрудник на работе или нет и когда ушёл. Суть следующая если `id` отсутсвует выводится пустой кортеж, если присутствует, но встречается один раз то выводится кортеж начинающийся с этого `id` и заканчивающийся остольными значениями кортежа, если `id` встречается 2 раза то выводится кортеж от первого момента встречи до последнего. Делается это засчёт `index` сначала мы ищем первое вхождение числа и определяем начальный индекс. Затем мы ищем конечный индекс, для этого делаем `start_index + 1` тоесть начинаем искать следующий индекс уже не посредственно со следующего значения от стартового. Возника проблема с ошибкой потому я применил конструкцию `try: except` которая работает по принципу если первое действие выдаёт ошибку или работает не коректно тогда делай то что в `except` своего рода инструкция на случай ошибки. вот и унас получается если мы не находим повторное значение `id` то оно будет равно длинне нашего кортежа и уже в конце функция вернёт нам значение кортежа начиная от стартового индекса до конечного.

## Самостоятельная работа №5

### есть некорая фирма которая хочет по id сотрудников видеть информацию о них. так же фирма хочет иметь возможность добавлять новую информацию о сторудниках

```python
from pprint import *
user = input("выберите действие 1: информация о сотруднике или 2: добавление нового сотрудника")

if user == "2":
    dic = {
        10: {"ключ": 43521, "доступ": True, "статус": "работает"},
        11: {"ключ": 53245, "доступ": True, "статус": "работает"},
        12: {"ключ": 14532, "доступ": True, "статус": "не работает"},
        13: {"ключ": 12342, "доступ": False, "статус": "работает"},
        14: {"ключ": 86797, "доступ": True, "статус": "работает"},
        15: {"ключ": 68734, "доступ": False, "статус": "работает"},
    }

    new_dic = dic.copy()

    while True:
        key = input("Введите новый ключ или нажмите Enter для выхода: ")
        if key == "":
            break

        new_info = {}
        new_info["ключ"] = int(input("Введите значение ключ-карты для {}: ".format(key)))
        new_info["доступ"] = bool(input("Введите значение доступа к засереченым материалам (True/False) для {}: ".format(key)))
        new_info["статус"] = input("Введите значение статуса для {}: ".format(key))

        new_dic[key] = new_info
    pprint(new_dic)

elif user == "1":
    req = int(input("введите id сотрудника"))
    dic = {
        10: {"ключ": 43521, "доступ": True, "статус": "работает"},
        11: {"ключ": 53245, "доступ": True, "статус": "работает"},
        12: {"ключ": 14532, "доступ": True, "статус": "не работает"},
        13: {"ключ": 12342, "доступ": False, "статус": "работает"},
        14: {"ключ": 86797, "доступ": True, "статус": "работает"},
        15: {"ключ": 68734, "доступ": False, "статус": "в отпуске"},
        None: {"ключ": None, "доступ": False},
    }
    res = dic.get(req)
    if not res:
        res = dic[None]
    k = res.get("ключ")
    c = res.get("доступ")
    b = res.get("статус")
    print("номер ключ-карты:", k, "Доступ к засереченым материалам", c, "статус сотрудника:", b)
else:
    print("не верное значение")
```

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_6/tema_6/5.1.jpg)

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_6/tema_6/5.2.jpg)

### Результат.

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_6/tema_6/5.3.jpg)

![Меню](https://github.com/DanilBorovyagin/software_engine/blob/Тема_6/tema_6/5.4.jpg)

## Вывод

В данном коде добавление сотрудников это больше илюзия нежели рабочая схема, да происходит добавление но мы не сможем найти новых сотрудников через цифру 2 для эжтого потребуется файл в котором будет хранится инфформация загружаться в него и выгружаться из него. в целом задача выполняется новый список выводится на экран, поиск по старому списку тоже работает. с помощью `pprint` выводим список более понятной форме. я мог бы расписать сам код, но это может занять весь лист а это краткий вывод

## общий Вывод по теме

В данной теме мы увидели как можно заменить большую конструкцию `if` `elif` всего лишь одним словарём данных.
Мы узнали как можно в словарь `kwargs` добавлять новые данные. 
Мы узнали как из обычных данных создавать списки и кортежи.
Узнали как можно короткой функцией присваивать данные определённым значениям в тексте вывода. 
Изучили `pprint` который выводит информацию более читабельной для человека если это касается больших текстов и объёмов данных.
Так же придумали свою задачу, в которой попытались реализовать, создание списка сотрудников по id и поиск информации о сотрудниках по id, но для создания и поиска информации в актуальном списке необходим файл в котором будет хранится информация, а это уже отдельная тема для изучения, вообще в кратце я представляю как это реализовать, но решил не бежать в переди паравоза.